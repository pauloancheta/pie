<%= simple_form_for @recipe, remote: true do |r| %>
  <%= r.input :name %>
  <%= r.input :category, as: :select, selected: ["Line"],
               collection: ["Line", "Dry Mixer", "Wet Mixer", "Sauce", "Stew", "Other"] %>
  <%= r.submit %>
<% end %>