<h4>
  <% if user_signed_in? && current_user.is_admin %>
    <%= link_to dish.name, menu_dish_path(@menu, dish) %>
  <% else %>
    <%= dish.name %>
  <% end %>
</h4>
<p><%= dish.description %></p>
<p><%= dish.price %></p>

<% if user_signed_in? && (current_user.id == @menu.user_id) %>
  <%= link_to "EDIT", edit_menu_dish_path(@menu, dish) %> |
  <%= link_to "DELETE", menu_dish_path(@menu, dish), method: :delete %>
<% end %>

<!-- <h2> This should be the menu index </h2>
<p> Menu description goes here </p>

<a id="addDish">Add New Dish</a>

<div id="newDishForm" style="display:none;">
  <%#= render "dishes/new" %>
</div>

<center>
  <%# @dishes.each do |dish| %>
    <div class="well">
      <h4><%#= link_to dish.name, dish_path(dish) %> </h4>
      <p><%#= dish.description %></p>
      <p><%= dish.price %></p>

      <hr>
        <%#= link_to "EDIT", edit_dish_path(dish) %> |
        <%#= link_to "DELETE", dish_path(dish), method: :delete %>
      <hr>
    </div>
  <%# end %>
</center> -->