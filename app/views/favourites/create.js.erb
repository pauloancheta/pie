$("#<%= @favourite.favourited_user_id %>").html('<%= link_to "Favourited", 
          user_favourite_path(current_user, @favourite.favourited_user_id), remote: true , 
          method: :delete, class: "btn btn-success btn-xs"%>')